[project]
name = "candles"
version = "0.1.0"
description = "A sample assignment to return OHLC points for a single symbol"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "uvicorn[standard]>=0.41.0",
]
